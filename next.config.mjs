/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["farmacia-guadalajara.nyc3.digitaloceanspaces.com"],
  },
};

export default nextConfig;
